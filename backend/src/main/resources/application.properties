spring.application.name=BizFlow Backend

# --- Server Config ---
server.port=8080
server.servlet.context-path=/api
server.error.include-message=always
server.error.include-stacktrace=on_param

# =========================================================
# C?U HÌNH DATABASE AIVEN (?Ã ?I?N S?N)
# =========================================================
# URL k?t n?i t?i Aiven Cloud (Port 20736)
spring.datasource.url=jdbc:mysql://bizflow-db-lphuc2324-d9c5.i.aivencloud.com:20736/defaultdb?sslMode=REQUIRED

# Tài kho?n & M?t kh?u
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
# T?ng th?i gian ch? k?t n?i lên 60s (vì m?ng ?i qu?c t? có th? ch?m h?n localhost)
spring.datasource.hikari.connection-timeout=60000

# --- JPA / Hibernate ---
# QUAN TR?NG: ?? 'update' ?? code t? ??ng t?o b?ng trên Aiven
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# --- Flyway ---
# T?t Flyway ?? Hibernate t? lo vi?c t?o b?ng trên Cloud cho d?
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true

# --- JWT Configuration ---
jwt.secret=TXlTdXBlclNlY3JldEtleUZvckJpekZsb3dQcm9qZWN0MjAyNXZlcnlMb25nU3RyaW5nToMakeItWork==
jwt.expiration=86400000
jwt.refresh-token-expiration=604800000

# --- Redis Configuration ---
# L?U Ý: Aiven gói Free không có Redis, nên ông v?n dùng Redis trên máy (Localhost)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=60000ms

# --- Logging ---
logging.level.root=INFO
logging.level.com.bizflow=DEBUG
logging.level.org.springframework.web=WARN
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# --- Encoding ---
server.tomcat.uri-encoding=UTF-8
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# --- Gemini API Key ---
gemini.api.key=AIzaSyDy4-uZehsd0q9lssDICo_AKCNKhxEaK4